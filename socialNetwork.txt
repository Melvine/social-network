// IMPORT CTIME
//
//
//include the linkedlist cpp and header file from cs 24
//work on template class

template <class DlinkedList>
class DoublyLinkedList {
  public:
    DoublyLinkedList();
    DoublyLinkedList(data);
    ~DoublyLinkedList();
    void Add(data);
    void Remove(data);
   
  private:
    Data data
};

template <class Data>
class DoublyLinkedList {
  public:
    Data();
    Data(data);
    ~Data();
};


class Wall {
    public:
      Wall();
      ~Wall();
      void add(WallPost);
      void remove(WallPost); 
      getWallList();    
    private:
      Vector <WallPost> WallList;
 
        
};
        

class WallPost {
    public:
        WallPost(message);
        WallPost();
        ~WallPost();
        
        void validateMessage();
        void makeMessage(message);
        void editMessage(message);
        void removeMessage(message);
        
        
    private:
        String message;
        time_t time;
        String author;
        int id;
        
            
};  

class User {
  public:
    User ();
    ~User ();
    User (username, password); 
    
    void editUserName(username);
    void editName(name);
    void editPassword(password);
    void removeWallPost(WallPost);
    String displayInfo();
    
    importUser();
  
  private:
    Wall userWall;
    String username;
    String name;
    String password;
    
}
  
  
class UserNetwork {
  public:
    UserNetwork();
    ~UserNetwork();
    void addUserusr(username);
    void removeUser(username);
    void findUser(username);
    String generateUserList();
    void fileToList();
     
  private:
    String username;
     
  
  
  
} 
